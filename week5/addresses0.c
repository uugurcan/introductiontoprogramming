// Prints an integer
// This is your starting point — a plain variable with a value.
// Run this and remember: every variable lives somewhere in memory.

#include <stdio.h>

int main(void)
{
    int n = 50;
    printf("%i\n", n);
}
